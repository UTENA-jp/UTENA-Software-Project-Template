# UTENA 全アプリ共通開発ルール

このファイルは、UTENAで制作する全アプリに共通する必須開発ルールです。
新規アプリはこのルールを初期状態から採用してください。
Codex・ChatGPT・その他の開発エージェントは、作業開始時に `PROJECT_STATE.md` とこの `DEVELOPMENT_RULES.md` を必ず全文確認してください。

## 開発の基本サイクル
1. 機能を実装する
2. 開発側でバグ・エラー・クラッシュ・データ不整合を確認して修正する
3. 開発側で実使用相当の通し確認を行う
4. 明らかな不具合を除去した後だけタカシ確認へ渡す
5. タカシから改善要望を受ける
6. 改善を実装し、再び2〜5を繰り返す

タカシ確認を開発側デバッグの代わりにしてはいけません。

## 回帰防止ルール

### 1. 重くならないこと
- 修正前後で性能を比較する
- クリック反応、スクロール、検索、追加・編集、保存、主要プレビュー／出力が以前の基準より悪化していないこと

### 2. できていたことが、できなくなっていないこと
- 今回触っていない既存機能も回帰確認する
- 保存、再起動、再編集、計算、検索、データ読込、プレビュー、出力など、既に通過した機能を壊さないこと

### 3. 修正済みバグを復活させないこと
- クラッシュ
- データ不整合
- 重さ・フリーズ
- クリックしても反応しない
- スクロール不良
- 画面リサイズ不具合
- 入力時フリーズ
- 保存不良
- プレビュー不良
- プレビューと実出力の不一致
- 不要なネット依存
- 不要な全件読込・materialization
を再発確認すること。

修正後に必ず回帰テストすること。
新しい機能が良くなっても、既存機能が悪くなった場合は作業完了扱いにしません。

**「一歩進んで二歩下がる」変更は禁止です。**
現在できていることを維持したまま、一歩ずつ着実に改善してください。

## 人間目線の確認
自動テスト成功だけでは完成ではありません。目的、入力欄、ボタン、操作順、可読性、プレビューと実出力の一致、保存結果を人間が説明なしで理解できるか確認してください。

## 作業開始時
1. `PROJECT_STATE.md` を全文読む
2. `DEVELOPMENT_RULES.md` を全文読む
3. branch・PR・未コミット変更を確認
4. 過去のP0/P1修正内容と性能基準を確認

## 作業終了時
1. 必要なテストと回帰テストを実施
2. `PROJECT_STATE.md` を更新
3. コードとドキュメントを同じcommitへ含める
4. 指定branchへcommit・push
5. 既存PR本文を更新
6. 未完了・タカシ確認待ちを明記

「ビルド成功」「自動テスト成功」だけで完成と報告してはいけません。
